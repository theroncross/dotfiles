- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.gitconfig:
    ~/.gitignore_global:
    ~/.vimrc:
    ~/.prettierrc:
    ~/.config/:
      glob: true
      path: config/*

- create: ~/.vim/undo-history

- shell:
    - [chsh -s $(which fish), Making fish the default shell]
